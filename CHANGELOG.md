# YeImageViewer 修改记录

## v1.36.18 - 2026-08-28

### 当前图片重命名

- 右键菜单新增“重命名”，F2 也可直接打开同一个原生 Windows 重命名窗口。
- 重命名时只编辑文件名主体并自动保留原扩展名；校验空名称、非法字符、末尾空格/句点、Windows 保留名称、过长名称和同名文件，避免覆盖已有文件。
- 重命名成功后同步当前图片路径、同目录自然排序、窗口标题、收藏状态和已保存的旋转信息，继续浏览时不需要重新打开图片。
- 新增文件名规则与真实磁盘改名单元回归，以及 F2、右键打开/取消、当前图片保持和交互恢复真实窗口回归。

## v1.36.17 - 2026-08-28

### 建立真实格式解码测试集

- 新增 44 种已声明静态/动图扩展名的真实文件，并统一纳入带来源、上游提交、许可证、SHA-256、尺寸和帧数断言的格式测试清单。
- 新增无界面的 `--decode-probe` 回归入口，测试会调用与正式浏览一致的 `ImageDatabase` 解码路径，拒绝把错误提示图当成解码成功。
- `runTests.ps1` 逐项校验样本哈希、解码退出码、图像类型、尺寸和最小帧数；同时要求清单覆盖除暂缺许可清晰样本的 LEP 外全部 `supportExt` 扩展名。
- 修复已声明支持的 OpenEXR 在默认环境下被 OpenCV 禁用的问题，并用上游 32 位浮点 EXR 文件锁定回归。
- 相机 RAW 使用单独的代表性实拍素材策略，避免为 40 余个相机后缀复制同一伪装文件；在取得许可清晰、体积可控的原始样本后继续补入。

## v1.36.16 - 2026-08-28

### 重构普通与沉浸浏览界面

- 按提供的界面设计统一普通与沉浸模式：增加常驻渐变图片信息栏、居中半透明底部工具栏，以及与工具栏联动显示的左右翻页按钮。
- 完整采用参考稿图标与分组，新增水平/垂直翻转、适应窗口、实际大小、沉浸显示、收藏、复制、删除、设置和缩放控制；悬停状态、活动状态、危险操作颜色和提示气泡与参考稿保持一致。
- 工具栏只在鼠标进入可见工具栏本体或左右翻页按钮的精确范围时出现，不再保留宽边缘或底部整条隐形热区；窄窗口下按比例缩小并保持居中。
- 新增统一的显示坐标变换，栅格图与 SVG 均支持水平、垂直翻转；收藏状态在当前会话中即时反馈。
- 新增 16 个界面图标资源、全部控件命中区域、窄窗口布局、翻转/旋转坐标和真实窗口居中悬停回归。

## v1.36.15 - 2026-08-28

### 阳光花朵相框应用图标

- 使用新的透明背景“阳光花朵相框”设计替换 YeImageViewer 应用图标、窗口图标、任务栏图标、文件关联图标和开始菜单快捷方式图标。
- 重新生成包含 16、24、32、36、48、64、96、128、256 像素的 Windows 多尺寸图标，并同步 README 展示图。
- 新增图标预览哈希、三份应用图标一致性、多尺寸目录以及 EXE 内嵌图标资源回归。

## v1.36.14 - 2026-08-28

### 原生清晰字体、固定设置窗口与精简图片信息

- 设置窗口参照提供的网页界面重构为固定 `620 × 620` 客户区；切换常规、文件关联、帮助、关于标签不再改变窗口大小，内容超出时使用细滚动条。
- 常规页采用分组卡片、Windows 风格开关和分段选项；文件关联页增加搜索与紧凑格式标签；帮助页改为分组的三列表格；关于页统一项目、上游和构建信息布局。
- 设置正文、图片信息、打印等非沉浸式自绘界面统一为 16 像素 Segoe UI，并通过 Windows 原生 ClearType 绘制，字号和清晰度与系统右键菜单接近；沉浸模式现有的逐像素自适应文字保持不变。
- 普通窗口图片信息改为约 60% 不透明黑色的紧凑卡片，只用足以托住文字的轻遮罩；仅显示文件名、格式、大小、分辨率及最多 6 项有效拍摄信息，不再把原始 XMP、MakerNote 等冗长内容覆盖在图片上，完整信息仍可复制。
- 新增固定窗口、跨标签尺寸、滚动边界、原生文字策略、图片信息筛选与遮罩颜色回归。

## v1.36.13 - 2026-08-28

### 设置页排版与文字清晰度

- 常规页改为双列紧凑布局：7 个开关按两列排列，4 组选项按两列对齐，减少右侧空白；顶部四个标签平均铺满窗口宽度。
- 帮助页彻底停用旧 JarkViewer 位图，改用 12 个双列快捷键卡片；中英文及深浅主题全部实时绘制，并统一为 18 像素字体。
- 设置、帮助、关于页文字统一字号；关于页不再单独使用超大标题。
- 增强 TrueType 灰度字形的中间覆盖率，减少小字号边缘发灰、发虚的问题。
- 沉浸模式的 EXIF 信息保持原有字号、逐像素自适应颜色和字形覆盖率；仅普通窗口改用按显示器 DPI 连续缩放的增强浅色文字与 2 像素阴影，不使用深色底板遮住图片。
- 透明棋盘格从 16 像素缩小为 12 像素，并把高反差的 `#DDDDDD / #FFFFFF` 调整为参考软件的柔和 `#EBEBEB / #F5F5F5`；深浅主题保持一致。
- 新增双列布局、标签宽度、帮助卡片、DPI 字号、字形覆盖率和 EXIF 阴影精确回归。

## v1.36.12 - 2026-08-27

### 区分沉浸与普通窗口背景

- 沉浸模式继续使用约 60% 不透明的黑色遮罩，让桌面适度透出。
- 普通带标题栏窗口的图片外区域改为不透明 `#7F7F7F` 中灰色，拉大窗口时不再透出桌面，贴近 Picasa 3 的分层表现。
- 修正将 DirectComposition 透明表面误判为沉浸状态的问题；首次打开、最大化进入沉浸以及退出后的背景现在由真实窗口模式决定。
- 用户选择的透明、白色、黑色或毛玻璃背景仍只作用于图片自身的透明区域。
- 新增精确像素回归，分别锁定沉浸遮罩 `0x99000000` 与普通窗口背景 `0xFF7F7F7F`。

## v1.36.11 - 2026-08-27

### 按当前图片恢复普通窗口

- 退出沉浸预览时，不再沿用进入沉浸时第一张图片的窗口尺寸，而是根据退出瞬间正在查看的图片重新计算普通窗口。
- 当前图片过大时继续遵守现有窗口约束：普通客户区宽、高均不超过当前显示器工作区的 90%；长图仍可在窗口内拖动浏览。
- 普通模式内继续浏览上一张、下一张时仍固定本次退回得到的窗口尺寸，避免边框忽大忽小。
- 新增 5 张不同宽高比的最小 SVG 夹具和真实窗口回归，覆盖“最大化进入沉浸、浏览到第 5 张、退出后使用第 5 张受限尺寸”。

## v1.36.10 - 2026-08-27

### 沉浸半透明黑色背景

- 取消 Windows 毛玻璃，客户区改用 DirectComposition 逐像素 Alpha 合成：图片外区域使用约 60% 不透明的纯黑遮罩透出桌面，图片内容保持不透明。
- 首次打开与普通窗口最大化统一调用相同的沉浸入口；首次显示前先完成无边框尺寸、透明表面和首帧绘制，减少普通窗口闪现及两次进入效果不一致。
- 普通带标题栏窗口继续使用同一透明客户区；窗口被拉大后，非图片区域仍是相同的半透明黑色，系统标题栏和缩放边框保持正常。
- 沉浸右上角关闭按钮不再依赖 SVG 解码结果，并在首次尺寸调整帧中直接绘制，首次打开也必须稳定可见。
- 新增合成表面策略、半透明黑色 Alpha 值、无重定向客户区以及沉浸/普通窗口真实交互回归。

## v1.36.9 - 2026-08-27

### 滚轮组合交互

- 新增 `Shift + 鼠标滚轮`垂直滚动当前图片，方便直接浏览超出预览区的长图。
- 新增 `Ctrl + 鼠标滚轮`切换上一张或下一张图片；同时按下 Ctrl 与 Shift 时优先切图，避免两个动作同时发生。
- 不带组合键的普通滚轮继续保留原有缩放及边缘、旋转区域的上下文行为。
- 设置帮助页和中英文 README 同步增加滚轮快捷键说明，并新增组合键方向、优先级与帮助布局自动回归。

## v1.36.8 - 2026-08-27

### 沉浸背景与窗口操作

- 沉浸模式右上角新增常驻 42×42 关闭按钮，使用与工具栏一致的 IconPark 线性图标；退回普通窗口后隐藏，由系统标题栏接管关闭。
- 普通窗口点击最大化或双击标题栏时直接重新进入无边框沉浸模式，不再进入 Windows 普通最大化状态。
- 沉浸模式的图片外区域固定使用 Windows 毛玻璃；用户选择的透明、白色、黑色或毛玻璃只作用于图片区域，普通窗口仍整体遵循用户背景设置。
- 退回普通窗口时同步结束 DWM 沉浸合成、清除鼠标状态并立即完整重绘，减少空白点击闪动及设置窗口移动引起的重绘变色。
- 新增沉浸关闭按钮、最大化进入沉浸、背景分层策略及设置窗口跨图片移动的自动回归。

## v1.36.7 - 2026-08-27

### 沉浸退回与设置界面

- 沉浸预览中单击图片不再退出，可继续按住左键拖动；单击图片外的背景空白或按 `Esc` 才退回带边框窗口，右键菜单保持可用。
- 退回后显式恢复窗口可见、启用、激活、键盘焦点和鼠标捕获状态，标题栏按钮与图片交互立即可用。
- 修正退出动作误发 `WM_DESTROY` 导致的窗口和后台进程残留，统一走正常的 `WM_CLOSE` 销毁流程。
- 设置页字体从 24 像素收紧为 18 像素，重新排列常规选项并消除“记住最后使用的显示器”与切图动画选项的重叠。
- 移除会误触打开文件关联设置的 `F2` 快捷键。
- 关于页显示构建时间和构建时自动嵌入的 Git Commit ID，方便核对安装包来源。
- 新增沉浸图片/背景点击、退回交互状态、标题栏关闭、设置页布局和构建元数据回归测试。

## v1.36.6 - 2026-08-27

### 沉浸预览

- 打开图片时默认使用覆盖目标显示器工作区的无边框沉浸预览，并通过半透明暗色画布与 Windows 毛玻璃弱化桌面背景。
- 参考 Picasa 3 的截图尺寸：小图按显示器 DPI 的 100% 逻辑尺寸显示；横图最多使用工作区 90% 宽度和 82.5% 高度。
- 竖图优先保证可读大小，只在宽度超过工作区 90% 时缩小；高度超出时默认显示图片中部，并保留上下拖动。
- 单击图片、按 `Esc`、`F` 或 `F11` 可退回带边框窗口；退回窗口客户区贴合首次打开图片的显示尺寸，此后切换图片不改变外框，并让每张图片继续使用各自的沉浸预览百分比。
- 新增三张用户示例尺寸对应的布局回归，以及真实无边框启动、点击退回和固定窗口切图回归。

## v1.36.5 - 2026-08-27

### 界面与操作

- 将上一张、下一张、左旋转、右旋转和设置合并为右下角横向工具条，统一使用 30×30 的 IconPark 线性图标并贴近窗口底部。
- 保留左右边缘的宽翻页热区，并让边缘提示图标与工具条风格一致。
- 移除左下角打印图标；打印仍可通过右键菜单或 `Ctrl+P` 使用。
- 全屏时按 `Esc` 恢复进入全屏前的窗口状态，最大化时按 `Esc` 恢复普通窗口。
- 设置页新增“Esc关闭图片”选项，默认关闭。
- 记住每个图片路径对应的旋转方向，关闭程序或切换图片后再次打开仍会恢复。
- 主窗口固定为 4:3：高度取当前显示器工作区的 90%，宽度按 4:3 计算；竖屏等宽度不足场景改为宽度最多 90% 后反算高度。图片能放下时保持 100%，仅在超出窗口时等比缩小，切图不改变窗口。
- 图片解码和最终窗口尺寸确定前保持主窗口隐藏，避免先闪现历史尺寸再跳到自适应尺寸。
- 设置页新增“记住最后使用的显示器”，默认开启；显示器断开或关闭选项时回退到主显示器，并兼容位于主屏左侧或上方的负坐标布局。

### 显示与稳定性

- 右键菜单新增透明、白色、黑色和 Windows 毛玻璃背景。
- 改善 SVG/draw.io 文字显示和矢量缩放，图片放大使用 Alpha 正确的双线性插值。
- 修正 Radiance HDR 红蓝通道顺序，并修复特定 DJI MotionPhoto 打开闪退。
- 为上述行为增加单元、像素、真实素材和窗口级回归测试。
